{
  "name": "novelkeeper-model-node",
  "version": "1.0.0+1",
  "description": "",
  "author": "John Ramberger",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NovelKeeper/novelkeeper-model-node.git"
  },
  "main": "dist/index.d.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npx nodemon",
    "format": "prettier --write .",
    "build": "rimraf dist && npm run format && tsc"
  },
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@types/node": "^20.4.5",
    "prettier": "^3.0.0"
  }
}
